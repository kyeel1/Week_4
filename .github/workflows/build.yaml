jobs:
  build:
    runs-on: ubuntu-20.04 
    steps: 
      - uses: actions/checkout
      - run: dotnet build

  #test:
  #analyze:
  #package:
  #deploy:

name: build

on:
  push:
    branches:
      - master